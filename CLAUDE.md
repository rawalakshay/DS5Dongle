<!--
  Not a second source of truth. AGENTS.md holds all project context for every AI agent;
  this file exists only because Claude Code loads CLAUDE.md and not AGENTS.md, and the
  @-import below pulls AGENTS.md into context at session start.

  Edit AGENTS.md. Add content here only if it applies to Claude Code and to nothing else.
  (Block-level HTML comments like this one are stripped before the file enters context.)
-->

@AGENTS.md
